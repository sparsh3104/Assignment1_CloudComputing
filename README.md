# Assignment1_CloudComputing

Live site: https://sparsh3104.github.io/Assignment1_CloudComputing/

This repository contains a static interactive site about Cloud Computing built with HTML, CSS and JavaScript.

Deployment: a GitHub Actions workflow (added to `.github/workflows/deploy.yml`) automatically publishes the repository contents to the `gh-pages` branch using the provided `GITHUB_TOKEN`.

If Pages is not already enabled in the repository settings, enable GitHub Pages source `gh-pages` -> `/` in the repository Settings → Pages.
Create and deploy website
